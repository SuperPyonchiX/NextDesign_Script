# プロジェクト共通指示

## Next Design の知識を引き継ぐ

- Next Design の調査・実装を始める前に `.local/nd-knowledge/index.md` を読む。対象に一致する記録だけをたどり、必要なときに診断原文を確認する。フォルダ全体を毎回読み込まない。
- 保存・更新の手順と書式は [知識の保存手順](docs/nd-knowledge.md) に従う。診断を受け取ったときに原文を保存し、構造や取得方法が分かった時点で知識と索引を更新する。保存のためだけに毎回ユーザーへ確認しない。
- 保存済みの知識で足りるなら再診断を求めない。不足・対象や版の違い・変更の申告・矛盾がある場合は、必要な範囲だけ確認する。保存日が古いという理由だけで再診断しない。
- プロファイルや版が不明な資料は未分類で保持する。名前の一致だけで別プロファイルに適用したり、観測していない情報を確認済みと扱ったりしない。
- 会社固有のメタデータ、診断の原文、要約、索引はすべて `.local/nd-knowledge/` に保存する。追跡対象の指示書・文書・テスト・コミットメッセージへ転記せず、Obsidian 等にも自動転記しない。保存先を強制 add しない。
- ローカル索引がない場合は保存手順に従って初期化する。既存の原文を探してから不足分を確認し、索引がないだけで診断の再出力を依頼しない。保存に失敗したら、その内容と理由を報告する。

<!-- repo-agent-bootstrap:begin -->
## エージェント共通運用

- `work/` はローカルの開発・検証作業記録用で、全体を Git 管理外とする。共有する運用手順は `docs/`、会社固有の診断・知識は `.local/nd-knowledge/` に保存する。`work/` を強制 add しない。
- 共通指示の正本は `AGENTS.md`。Claude は `CLAUDE.md` から参照する。
- プロジェクト固有スキルは `.agents/skills/` を編集する。`.claude/skills/` は生成物で、直接編集しない。
- 正本を変更したら `powershell -NoProfile -File Tools/Sync-AgentSkills.ps1` を実行し、正本と配布物を同じ変更に含める。
- `powershell -NoProfile -File Tools/Test-AgentHarness.ps1` で指示の入口・スキル構造・配布物を検査する。PowerShell 7 では `powershell` を `pwsh` に置き換える。
- 指示・スキル・配布物・Tools の共通ハーネススクリプトを Git 管理する。個人設定や認証情報は含めない。
<!-- repo-agent-bootstrap:end -->
