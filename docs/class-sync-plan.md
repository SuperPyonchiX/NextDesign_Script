# クラス図の差分同期：実装計画と進捗

[シーケンス図の計画](sequence-sync-plan.md)と同じ進め方を、別拡張 `ClassImportProbe` で繰り返した。共有部品を切り出さずに写したのは、クラス図で得た知見を独立に積み上げるため。2026-09-22 に PlantUmlTool 2.2.0 へ統合し、ClassImportProbe は削除した（版履歴は [class-sync-history.md](class-sync-history.md)）。

## 完成条件

表示中のクラス図で「PlantUMLを反映」を押し、ファイルを選ぶと、対応付け・照合・反映・読戻しまで完了する。PlantUMLを意味上の正本とし、共通部分のモデルID・図形ID・表示設定を保持する。図形の座標は新しい要素にだけ与え、既存要素は動かさない。図に載っていないモデルは変更しない。

対象はクラス（キーワード・ステレオタイプ）、属性、操作、列挙リテラル、参照関連（汎化・実現・依存・集約・合成・関連）、所有先（package・図上の親ノード）。識別子コメントをPlantUMLに追加しない。プロジェクトを自動保存しない。反映は一つのトランザクションに収め、失敗時は取消と復元の読戻しを行う。

## 前提

- クラス図は `EditorType == "ERDiagram"`（一部 `"TreeDiagram"`）で型付きアクセサがない。`IDiagram.Nodes / Connectors` とモデル側のメタクラス・フィールドで意味を取る。読取り経路は PlantUmlTool の出力処理と同じ規則を写した。
- 実機プロファイルのクラス図メタモデル（属性・操作・関連のフィールド名）は未記録。対応表は推測から始め、「クラス図調査」の実測で埋める。実測値はローカル知識へ保存し、この文書には書かない。
- Editor JSON の `Compartments` は実機では区画の表示状態だけで、メンバごとの図形を持たない（公式サンプルの `Items[].ModelId` はこのプロファイルに無い）。属性・操作の追加削除はモデル操作だけで図に反映される見込み（未検証）。
- ImportUnitFromJson・関連の順序番号・Undo/Redo・エディタ再描画の規則はシーケンス図で観測済みだが、すべて `SequenceDiagram` エディタでの観測。ERDiagram で同じかは検証する。

## 0.1.0で実装した範囲

| 部分 | 実装・検証状態 |
|---|---|
| 共通構造 | `ClassDocument`：図・package・class・attribute・operation・literal・link。所有と接続先を検査 |
| 入力 | `ClassPumlParser`：PlantUmlTool の出力文法。統合線の分解。表示指定は無視して記録 |
| 書出し | `ClassPumlWriter`：出力側と同じ並び・文法。サンプルでバイト一致を検査 |
| 差分計画 | `ClassSyncPlan`：名前の一意アンカー、兄弟列のLCS、単独未対応の改名、関連の両端照合 |
| SDK読取り | `ClassDiagramSnapshot`：ノード順・別名・メンバ・フィールド由来の関連・コネクタ由来の線 |
| 調査 | `ClassDiagramProbe`：メタクラス・フィールド・関連・座標・Editor JSON の退避 |
| 実行操作 | 「クラス図調査」「差分を検証」。読取り専用 |
| 図への反映 | **未実装** |

0.1.4 時点の実機検証: ゲートA・B 通過。改名 1 件の入力で `update attribute [name]` 1 件を実機確認。追加・削除・型変更の入力は未確認。

## 実装順と実機ゲート

1. **ゲートA（調査）**: 通過（2026-09-21、0.1.0）。メタクラス・フィールド・コネクタ・Editor JSON を観測し、ローカル知識へ保存した。対応表は PlantUmlTool の既定表と配置先の矢印表で足りた。
2. **ゲートB（往復）**: 通過（2026-09-21、0.1.4）。ノード 78・コネクタ 59 の図で、未編集出力との差分候補 0 件。0.1.1〜0.1.4 で直した読取り規則: 括弧を含む属性名、class 系ノードの入れ子の展開、メンバの描画行比較、匿名フィールドの同一行、矢印を比較しない、ラベルだけで関連を対応付ける。
3. 0.2.0: 属性・操作の**名前**の書き戻し。**実機で試行・確定とも成功（2026-09-21、Property 1 件）。** 本体の Undo/Redo・保存再読込後の差分 0 件も確認済み。同一クラスの複数改名（属性＋操作）の試行と、型変更混在時の停止は 0.2.1 で実機確認済み。改名だけの計画を受理し、`SetField("Name")` → 読戻し → 図全体の再読取りで 0 件 → 試行は必ず取消、確定は照合後に Commit。型・可視性は実値観測後に別段階で扱う。
4. 0.2.2: 可視性（値フィールド、`Public` / `Private`）と型（`Type` 参照を `UnRelate`/`Relate` で既存の型モデルへ付け替え。同名が無い・複数なら停止）の更新。**実機で試行・確定・Undo/Redo・保存再読込まで成功（2026-09-21）。** 型モデルの新規作成は 0.4.0。
5. 0.3.0: 既存クラス間の参照関連の追加削除（`Relate` / `UnRelate`）。削除は実機で成功（2026-09-21）: UnRelate 1 回で両端フィールドが消え、コネクタ図形も自動で消える（K027）。0.3.1 で対の行を自動補完し、1 行削除・1 行追加の試行と追加の確定が実機成功。製品が作るコネクタは Association モデル付きだが IsVisible=False で描かれない（K029）。0.3.3 の SetVisible(true) は読戻しでは真だが保存形式の IsVisible は偽のまま（K032、手動の線との差はこの 1 項目）。ShowShape も同じ（K033）。SDK のフラグ操作は保存形式に届かないため、0.3.5 で Editor JSON の再インポート（退避した Editor に同 ID のコネクタを IsVisible: true で足して Editors だけ再反映）に切り替え。再反映は成功するが取消で戻らない（K034）ので 0.3.6 で確定時のみ実行。**確定で線の描画を実機確認（2026-09-21、K035）。** Undo/Redo は効くが表示反映は図の切り替えが必要。保存再読込後の差分 0 件も確認。Navigated / SuperClasses の試行も成功（K036）。
6. 0.4.x: 属性・操作の追加削除。**属性追加（K039）・操作追加（K040）・属性削除（K041）の試行が実機成功（2026-09-21）。** メタクラスは IClass 渡し（K038）。**途中挿入（AddNewModelAt）・確定・Undo/Redo・保存再読込まで実機成功（K042）。** **操作追加・属性削除の確定、追加と削除の混在、原本への全戻し、保存再読込後の差分 0 件まで実機成功（K043）。** 型モデルの新規作成・引数付き操作・クラスの追加削除は後続。`AddNewModel` + `IDiagram.AddNodeShape` と Editor JSON 再反映のどちらが図形・区画を正しく作るかを検証する。
7. 0.5.0〜0.6.5: 残りの構造差分（すべて実機成功）。(a) 引数付き操作の追加と既存操作の引数の改名・追加は**確定まで実機成功（K044）**。参照されている操作の削除は停止（意図どおり）。優先順は (a) 引数付き操作の追加（`Parameter` 配下に Argument を作る）、(b) 型モデルの新規作成は 3 種とも試行・確定が実機成功（K048〜K051）、(c) クラスの追加は、モデル作成と `AddNodeShape` による可視ノードまで実機成功（K053）。同名型の選択（0.6.1）、製品が張る匿名参照の補完と新クラスのメンバのメタクラス（0.6.2）を修正。**クラス追加の確定（図に箱が出る）・削除・保存再読込後の差分 0 件まで実機成功（K056）。** 矢印形の違う再出力でも差分 0 件（K057）。**0.6.4 のノードごと削除で孤立ノード 0、関連付きの追加と削除の確定も成功（K058）。** **図の外（シーケンス図のライフライン）から参照される既存クラスの削除は書込み前に停止することを実機確認（K060）。** 計画にあった全項目が実機で確認済み。
8. 0.7.0: 通常操作「PlantUMLを反映」を追加（実装済み、実機待ち）。本体を `ClassSyncRuntime.Run` に切り出し、MCP から呼べる形にした。
9. 0.7.1: クラスの改名、操作の戻り値（`Type` 参照、片側比較）、属性の多重度（`LowerBound/UpperBound`、片側比較）・既定値（`Default`）。実装済み、実機待ち。
10. NdMcp 0.2.0: クラス図更新の API を追加。**実機で editors / current / preview / trial / apply（図を閉じた状態、クラス追加・関連追加を含む）まで成功（2026-09-22、K061〜K063）。**`ClassSync.cs` / `ClassSyncRuntime.cs` を `build_main.py` で転記し、`GET /class-sync/editors|current`、`POST /class-sync/preview|trial|apply` と MCP ツール `nd_class_diagram_*` を用意した。修正は ClassImportProbe 側で行い、NdMcp は再生成するだけにする。
11. PlantUmlTool 2.2.0 への統合（2026-09-22、**実機確認済み**: 出力→無編集検証 0 件、試行、反映と Undo、一括出力）: 同期本体を `PlantUmlTool/src/60-class-sync.cs` / `61-class-sync-runtime.cs` に移し、リボン「反映（クラス図）」を追加。クラス図の出力を同期側の Snapshot + Writer に切り替え、出力→無編集比較が 0 件になることを構成で保証する。NdMcp はここから転記。ClassImportProbe は削除済み。シーケンス図は SequenceImportProbe の検証完了後に同じ形で統合し、状態遷移図はその後に新規で作る。
12. 転記元の一本化（2026-09-22、実機待ち）: AgentReview 0.13.3 と NdMcp 0.2.1 の PlantUML 出力部を `PlantUmlTool/src`（10 / 40 / 50 / 60 / 61、NdMcp は 63 も）からの転記に切り替え、AgentReview の `main.cs` も `tools/build_main.py` の生成物にした。3 拡張のクラス図 .puml が同じ書式になる。

### 2026-09-21 時点の到達点（同日夜に更新）

| 差分 | 試行 | 確定 | Undo/Redo | 保存再読込 |
|---|---|---|---|---|
| 属性・操作の名前 | ○ | ○ | ○ | ○ |
| 属性の可視性・型 | ○ | ○ | ○ | ○ |
| 関連の追加（線の表示込み）・削除 | ○ | ○ | ○（表示は図の切替が必要） | ○ |
| 属性・操作の追加（途中挿入）・削除 | ○ | ○ | ○ | ○ |
| 追加と削除の混在 | ○ | ○ | 未 | ○ |
| 操作の引数（追加・改名・追加削除） | ○ | ○ | ○ | ○ |
| 型モデルの新規作成（既定・種類指定・引数の型） | ○ | ○ | ○ | ○ |
| クラスの追加（メンバ・関連付き）・削除 | ○ | ○ | ○ | ○ |
| クラスの改名・所有先変更、package の追加削除、操作の戻り値、属性の多重度・既定値 | 停止（対象外） | | | |

## 未対応の構造差分に対する方針

| 差分 | 方針 |
|---|---|
| 図に載っていないクラスへの関連 | 対象外。要照合項目として報告する |
| 所有関連（`IsEmbedded`）の線 | 読まない。出力側と同じ |
| 自己参照 | 読まない。出力側と同じ |
| 同名メンバの重複 | 兄弟列の並びで対応付ける。曖昧なら再作成候補 |
| クラスの宣言順 | 差分にしない。座標が決める |
| 所有先の変更（package 間の移動） | `move` として検出。反映は 0.4.0 以降 |

## 横断監査で確認した問題

| ID | 問題・不足 | 対応状況／次の検証 |
|---|---|---|
| B01 | 実機メタモデル未記録 | ゲートAで観測し、対応表を実測値へ置き換える |
| B02 | 統合線の分解が手書き入力で誤読しうる | README に規則を明記。ゲートB後に入力の実例で確認 |
| B03 | 未対応メタクラスを属性扱いにする既定 | 要照合項目として必ず表示。黙って捨てない |
| B04 | 出力側の並び規則（座標順・別名の衝突接尾辞）への依存 | 写した規則をサンプルで検査。実機の一致はゲートBで確認 |
| B05 | 属性名・型定義名の括弧を操作と誤読 | 0.1.1 で操作の形を限定、0.1.2 でメンバを描画行で比較。実機で解消 |
| B06 | class 系ノードの入れ子を図上の親で読んでいた | 0.1.2 で出力側と同じ展開に変更。実機で解消 |
| B07 | 矢印表がリポジトリの PlantUmlTool と配置先で異なる | 配置先の実出力に合わせた（0.1.4）。矢印は比較しない。2026-09-21 夜の再出力はリポジトリ版の形（矢印表なし）だった（K056）。`_current.puml` のテキスト比較は版に依存するため目安にとどめる |
| B08 | 同方向で矢印だけ違う2本の関連が対応付かない | 0.1.4 でラベルだけで対応付け。実機で解消 |
| B09 | 属性と操作を同じ種類にしたため同一クラスの複数改名が一意にならない | 0.2.1 で名前以外の描画→種類→メンバ全体の 3 段で対応付け。実機で解消 |

## 検査とソース構成

`PlantUmlTool/src/60-class-sync.cs`（純粋部）/ `61-class-sync-runtime.cs`（SDK 依存部）/ `62-class-sync-ui.cs` が正本。`main.cs` は生成物で直接編集しない。NdMcp は `NdMcp/tools/build_main.py` が 60/61 を転記する。

```powershell
python PlantUmlTool/tools/build_main.py
python PlantUmlTool/tests/run_class_sync_tests.py
python PlantUmlTool/tests/compile_sdk.py --sdk-root work/sequence-api-research
python NdMcp/tools/build_main.py
```

診断レポートは実行PCの `%LOCALAPPDATA%\NextDesign.ClassSync\reports` へ保存する。モデル名やIDを含むため、公開リポジトリへ追加しない。
