# クラス図同期実験（ClassImportProbe）の版履歴と実機手順

ClassImportProbe は 0.1.0〜0.7.2 でクラス図の PlantUML 同期を実機検証するために使った実験拡張。2026-09-22 に同期本体を PlantUmlTool 2.2.0（`PlantUmlTool/src/60〜62`）へ統合し、拡張フォルダは削除した（履歴は git にある）。以下は当時の README をそのまま残したもので、扱える差分・停止条件・各版の実機手順の記録として参照する。ボタン名は PlantUmlTool では「差分を検証」「PlantUMLを反映」「試行して戻す」「診断表示」に対応する。

---

## 0.7.2: 括弧を含む戻り値

NdMcp 0.2.0 の実機（`/class-sync/current` → 無編集で `/class-sync/preview`）で、`+ Connect(a, b) : decltype(Skeleton::Connect(a,b))` のように戻り値に括弧を含む操作 8 件が「引数の更新」として出た。操作行の正規表現が引数を最後の `)` まで貪欲に取っていたため。最初の `(` と対応する `)` までを引数とし、その後ろの `: 戻り値` に括弧があってもそのまま戻り値にする。`uint8 (raw)` のような括弧付きの型や `width (mm)` のような括弧付き属性名の扱いは変えない。


## 0.7.1: クラスの改名、戻り値、多重度、既定値

| 差分 | 書き方 | 比較 |
|---|---|---|
| クラスの改名 `class "新名" as 別名` | クラスの `SetField("Name")`。別名は変えても変えなくてもよい（照合は名前で行い、同じ所有先で 1 件だけ対応が付かないクラスを改名と見る） | 対称 |
| 操作の戻り値 `+ op() : T` | 操作の `Type` 参照を既存の型モデルへ付け替え（無ければ型定義を作る。`T <<Kind>>` で種類指定） | **片側**（下記） |
| 属性の多重度 `[a..b]` | `LowerBound` / `UpperBound` へ SetField（`*` は上限 -1） | **片側** |
| 属性の既定値 `= x` | `Default` へ SetField | 対称 |

PlantUmlTool は戻り値と多重度を出力しない（このプロファイルのフィールド名が出力側の候補に無い）。図から読んで対称に比較すると、未編集の出力で偽の差分が出る。そこで戻り値と多重度は「入力に書いてあるときだけ比較する」片側比較にした。入力に書かなければ図の値はそのまま、書けば図がその値になる。消す操作はテキストからはできない（`: void` のように別の型を書く）。既定値は出力にも出るので対称。クラスのキーワード・ステレオタイプの変更は引き続き停止する。

### 実機手順（0.7.1）

1. `クラス図_class_new.puml` のコピーで `class "TimerWrapper" as TimerWrapper` の名前を `"TimerWrapper2"` に変えて「PlantUMLを反映」。フォームで名前が変わっているか。他の図（シーケンス図）のライフラインの表示名も追従しているか。
2. 別のコピーで `+ Start(...)` の行末に ` : bool` を足して「PlantUMLを反映」。フォームの「型」に bool が入るか。
3. 別のコピーで `- m_AppName : std::string` を `- m_AppName : std::string [0..*] = "x"` にして「PlantUMLを反映」。フォームの多重度下限 0・上限 -1（または *）、初期値 "x" になるか。
4. それぞれ Ctrl+Z / Ctrl+Y と、`クラス図_class_new.puml` で反映して元に戻す。

## 0.7.0: 通常操作「PlantUMLを反映」

実機で全種類の差分が往復したので、通常の操作を 1 ボタンにした。「反映」グループの「PlantUMLを反映」は、ファイルを選ぶ → 差分を表示して確認 → 反映 → 読戻しで照合 → 確定、を一度に行う（内部は「更新を確定」と同じ経路で、結果の見出しだけ「PlantUMLの反映」）。差分がなければ何もしない。「更新の検証」グループの「更新を試行して戻す」「更新を確定」は検証用にそのまま残す。

内部も整理した。比較と反映の本体は `ClassSyncRuntime.Run(app, editor, pumlText, ...)` で、ファイル選択や結果ダイアログを持たず、対象の図を引数で受け取り、確認はコールバックで行う。リボンの各ボタンはその薄い包み。MCP など別の入口から同じ処理を呼ぶための準備。

## 0.6.5: クラスの宣言を消しただけの入力を受ける

クラスを消すために宣言だけ消した入力は、その別名を使う関連行が残るため E120「未宣言の別名」で止まっていた。クラスの関連はクラスと一緒に消えるので、宣言のない別名（英数字と `_` だけ）を使う関連行は読み飛ばし、要照合に件数を出す。関連行まで消した入力も従来どおり通る。別名の形をしていない未宣言（引用符や記号を含む）は従来どおり E120 で止める。

## 0.6.4: クラスの削除はノードごと消す

**実機確認済み（2026-09-21）**: 関連付きのクラスの追加・確定、ノードごとの削除（孤立ノード 0）、削除後の差分 0 件。これで計画にあった差分の種類はすべて実機で往復した。

クラスの追加・削除の確定と、矢印表現の違う再出力での差分 0 件を実機確認した。削除後の読取りに「モデルのないノード 1 件」が出たため、クラスの削除はノード図形の `Delete(true)`（図形とモデルを一緒に削除）で行い、ノードが無いクラスだけモデルを直接消す。診断に削除後の孤立ノード数を出す。

## 0.6.3: クラス追加に Editor 退避を要求しない

クラス追加の試行は 3 段階一致した。試行の直後に確定すると、取消後は未保存扱いになるため Editor 退避のエクスポートが拒否された。クラスのノードは `AddNodeShape` で可視に作れることが分かったので、Editor 退避は関連の追加を含むときだけ行う。クラス追加だけなら試行→確定の間に保存は要らない。`AddNodeShape` がノードを返さなかった場合は C230 で停止する。

**試行の直後に関連の追加を確定するときは、間に Ctrl+S で保存する。** 図の内容は試行前と同じなので保存して問題ない。

## 0.6.2: 新しいクラスに製品が張る匿名参照を期待に含める

クラス追加の試行で、クラス・ノード・属性・操作の作成は通り、残差は「図にあり入力にない `NewUnit --> OnBoardClientApp`（匿名フィールド）」の 1 件だけだった。package 配下にクラスを作ると製品が所有関係の裏返しの匿名参照を自動で張り、PlantUmlTool はそれをラベルなしの線として出す。追加したクラスに触れる匿名フィールドの線は、更新後の読戻しから期待文書へ補う。

あわせて、新しいクラスのメンバのメタクラスは隣のクラスのメンバから取る（既存クラスは `Method` で操作を持つのに、フィールドの型 `Operation` で作られていた）。

## 0.6.1: 同名の型が複数あるときの選び方

クラス追加の試行で、クラスのモデルと `AddNodeShape` によるノード（可視）は通ったが、属性 `m_Value : int` の型解決が「`int` が 3 件」で止まった。`int` のようなプリミティブはクラスごとに型定義として持たれていることがあり、新しいクラスの配下には無いため、これまでの「同じクラス配下→上位」の絞り込みが効かなかった。同名の候補が複数残ったときは、同じ所有先の兄弟クラスが参照しているもの → 所有先が最も浅いもの → それでも同数なら所有先の名前順で先頭、の順で 1 件に決める（診断に選んだ根拠を出す）。停止はしない。

## 0.6.0: クラスの追加削除

最後の構造差分。「更新を試行して戻す」「更新を確定」の対象にクラスの追加と削除を加えた。

| 操作 | 書き方 |
|---|---|
| 追加 `class "名前" as 別名 <<Unit_Impl>> { ... }` | 入力で同じ所有先（package か図上の親ノード）にある既存クラスのうち、キーワードとステレオタイプが同じで一番近いものを「隣」とし、その所有先・所有フィールド・メタクラスで `AddNewModel` → `Name`。ノードは `IDiagram.AddNodeShape` を試し、隣のノードの右 40px に置く。確定時は Editor JSON 再反映で隣のノード要素を複製して可視にする（API でノードが作れなかった場合の保険） |
| 削除（宣言を消す） | クラスのモデルを `Delete()`。メンバと図上の関連は一緒に消える。図の外（シーケンス図など）から参照されていれば停止 |

追加したクラスの属性・操作・関連は同じ入力に書けば同時に作られる（クラスの作成後に解決する）。クラスの改名・所有先の変更、package / component の追加削除、新しいクラスの中への入れ子は停止する。同じ所有先に既存クラスが無い場合も停止する（種類と配置を決められないため）。

### 実機手順（0.6.0）

1. 原本のコピーで、`UDSLayerServiceImpl` の宣言（`class "..." as UDSLayerServiceImpl <<Unit_Impl>> {` から `}` まで）の直後に次を足して「更新を試行して戻す」。
   ```
   class "NewUnit" as NewUnit <<Unit_Impl>> {
     - m_Value : int
     + Init()
   }
   ```
   診断の `class target: add`、`created class`、`AddNodeShape:`、`class node ... visible=` の行。
2. 通ったら「更新を確定」。図に `NewUnit` の箱が隣に見えるか。Ctrl+Z / Ctrl+Y。保存して開き直し → 差分 0 件。
3. 原本で「更新を確定」し `NewUnit` を消す（削除の確認）。箱と関連が消えるか。

## 0.5.4: 引数の比較は名前だけ

`Run(cfg : Config <<PointerType>>)` の入力で型付き引数の作成が通ったが、PlantUmlTool は引数を名前だけで出すため、図側の `Run(cfg)` と入力の `Run(cfg : Config ...)` が「引数の差」として残っていた。比較に使う引数は名前の並びに正規化し、`: Type <<Kind>>` は書込み専用にした（既存の操作の引数に型を書き足しても差分にはならない。型を付けたいときは操作の追加時か、引数の名前を変える更新と一緒に書く）。

## 0.5.3: 作った型定義の行を期待文書に補う

0.5.2 の型作成は成功したが、PlantUmlTool はクラス配下の型定義を記号なしの属性行として出す（`EventParams` などと同じ）ため、作った型が読戻しで属性行 1 本として現れ、入力に無いので残差になった。型を作ったときは、その型定義の行を所有先クラスのメンバ末尾に補って照合する。順序残差は許容する。

次回以降に PlantUmlTool で出力し直すと、その行は普通に出力に含まれる。

## 0.5.2: 型モデルの新規作成

属性・引数の型に、プロジェクトに無い名前を書いた場合、これまでは C220 で停止していた。0.5.2 では所有先クラスの型定義フィールドに名前だけの型モデルを作って参照する。DeSIDE の型定義は 10 種（`ImplementationDataType` / `StructureType` / `PointerType` / `NumericalType` / `StringType` / …）あり入力からは決まらないので、既定は `ImplementationDataType`。別の種類にしたいときは型名の後ろに `<<StructureType>>` のように書く（比較では無視される）。

```
- m_Extra : NewType                      → ImplementationDataType "NewType" を作る
- m_Point : Point <<StructureType>>      → StructureType "Point" を作る
+ Run(cfg : Config <<PointerType>>)      → 引数の型も同様
```

メタクラスは同じフィールドの既存の型定義と同じもの（無ければフィールドの型クラス）。同じ実行で同じ名前を複数回使っても 1 件だけ作る。既存の同名の型があれば作らず参照する（同名複数は停止）。所有先クラスにそのフィールドが無ければ、使えるフィールド名を列挙して停止する。

参照されている操作の削除は 0.5.1 の方針のまま停止する（参照元はシーケンス図のメッセージと実機確認）。

### 実機手順（0.5.2）

1. 原本のコピーで 17 行目の型を `std::vector<ConvertErrCodeType>` から `NewType` に変えて「更新を試行して戻す」。診断の `type 'NewType' not found: will create ...` と `created type ...` の行。
2. 別のコピーで `    - m_Point : Point <<StructureType>>` を足して試行。`created type StructureType` になるか。
3. 通ったら確定 → フォームで型が入っているか、クラス配下に型定義が増えているか → Ctrl+Z / Ctrl+Y → 保存して開き直し → 差分 0 件。

## 0.5.1: 参照されているメンバの削除停止に参照元を出す

引数付き操作の追加・引数の改名・追加は確定まで実機確認済み。既存の操作を消す入力は「4 件の参照先になっている」で停止した（他の図のメッセージ等がその操作を参照しているときは、クラス図側の入力だけで消さない方針）。停止理由に参照元の種類・名前・フィールド・所有先を最大 5 件出す。自分の引数からの参照は数えない。

## 0.5.0: 操作の引数

PlantUmlTool は操作の引数を名前だけ `Connect(transportInfo, appName)` の形で出す（引数は `Parameter` 配下の Argument モデルで、型は出力されない）。入力の括弧の中を `,` で分けた名前の並びとして扱い、次を書けるようにした。

| 操作 | 書き方 |
|---|---|
| 引数付きの操作の追加 `+ Reset(a, b)` | Method を作った後、`Parameter` に Argument を名前順に `AddNewModelAt` / `AddNewModel` で作る |
| 既存操作の引数の変更（改名・追加・削除） | 件数が同じなら順に `Name` を書き換え、違えば余分を `Delete` し足りない分を作る |
| 引数付き操作の削除 | 操作ごと `Delete()`（引数は所有子として一緒に消える） |

Argument のメタクラスは既存の引数（同じ操作、同じクラスの別操作、プロジェクト内の順）から取り、無ければ `Parameter` フィールドの型クラス。手書きで `a : Type` と書いた場合は型モデルを名前で解決して `Type` 参照を張る（無ければ停止）。戻り値は引き続き扱わない。

### 実機手順（0.5.0）

1. 原本のコピーで操作行の直後に `    + Reset(mode, force)` を足して「更新を試行して戻す」。診断の `arguments: field=... class=...` と `created argument` の行。
2. 既存の操作の引数を 1 つ改名（例 `Connect(transportInfo, appName)` → `Connect(transportInfo2, appName)`）して試行。
3. 既存の操作に引数を 1 つ足す／消すで試行。
4. 通ったら確定 → フォームの引数一覧 → Ctrl+Z / Ctrl+Y → 保存して開き直し → 差分 0 件。

## 0.4.2: メンバを入力の位置に挿入する

**実機確認済み（2026-09-21）**: 属性の途中挿入・操作の追加・属性の削除、追加と削除の混在、原本での全戻し、それぞれの確定と Undo/Redo・保存再読込後の差分 0 件。残りはクラスの追加削除・引数付き操作・型モデルの新規作成で、いずれも C231 で停止する。

0.4.1 で属性の作成・名前・型の書込みは通ったが、`AddNewModel` は所有コレクションの末尾に足すため、入力で途中に書いた属性は既存メンバの順序残差 1 件で試行が取り消された。入力でその属性の直後にある既存メンバの位置に `AddNewModelAt(field, class, "before", index)`（SDK 注記では実験的）で挿入する。失敗したら末尾追加に戻し、その場合だけ更新後の照合でメンバの順序残差を許容する。

## 0.4.1: メンバ作成のメタクラス指定を修正

`AddNewModel("Attribute", "Property")` が「存在しないクラス 'Property'」で失敗した（短い名前は受け付けない。試行の取消・復元は一致）。同じフィールドの既存メンバの `Metaclass`（無ければフィールドの `TypeClass`）を `AddNewModel(IField, IClass)` に渡す形に変えた。

## 0.4.0: 属性・操作の追加削除

「更新を試行して戻す」「更新を確定」の対象に、既存クラス配下の属性・操作の追加と削除を加えた。

| 操作 | 書き方 | 前提 |
|---|---|---|
| 属性の追加 `- name : Type` | 所有先クラスの `Attribute` フィールドに `AddNewModel` → `Name` → 可視性 → 静的 → `Type` 参照を既存の型モデルへ | メタクラスは同じフィールドの既存メンバと同じもの（無ければフィールドの型）。型は省略可。多重度・既定値付きは停止 |
| 操作の追加 `+ name()` | `Operation` フィールドに `AddNewModel` → `Name` → 可視性 | **引数なし・戻り値なしだけ**。引数は `Parameter` 配下の別モデルなので後続 |
| 削除（行を消す） | 対象モデルの `Delete()` | 他から参照されておらず（型定義として使われている子は停止）、子モデルを持たないこと |

メンバの図形は Editor JSON に無い（区画は表示状態だけ）ため、モデル操作だけで区画に反映される見込み。クラスの追加削除は引き続き停止する。

### 実機手順（0.4.0）

1. 原本のコピーで、17 行目の直後に `    - m_Extra : std::string` を足して「更新を試行して戻す」。期待はメンバ追加 1 で 3 段階一致。
2. 同じく 35 行目付近の操作の直後に `    + Reset()` を足して試行。
3. 原本のコピーから既存の属性行を 1 行消して試行（型定義の行ではなく `- m_AppName : std::string` のような Property の行）。
4. 通ったら確定→区画に出るか→Ctrl+Z / Ctrl+Y→保存して開き直し→「差分を検証」0 件。

## 0.3.6: 線の表示は確定でだけ行う

0.3.5 の Editor 再反映は成功し（同 ID で上書き、コネクタ数不変）、読戻しも一致したが、試行の取消で図が処理前に戻らなかった。Editor の再反映はトランザクションの取消で元に戻らない。試行は関連の書込みだけを確認し、線を表示にする再反映は「更新を確定」でだけ行う。

**実機確認済み（2026-09-21）**: 確定で線が描画された。本体の Undo/Redo は効くが図の表示には即時反映されず、別のモデルへ切り替えて戻すと反映される（Editor 再反映で置き換えた図形の描画が遅れる。回避策は未着手）。

試行の取消が完全でなかった後は、保存せずにコピーを開き直してから確定に進む。`HasUnsavedChanges` が偽でもエクスポートが「先に保存」で拒否されることがあるため、その場合も同じ案内を出す。

## 0.3.5: 追加した関連の線を Editor JSON の再反映で表示にする

`SetVisible` も `ShowShape` も SDK の読戻しでは真になるが、保存形式の `IsVisible` には届かず線は描かれなかった（手動の線との差はこの 1 項目だけ）。SDK のフラグ操作をやめ、シーケンス側で実証済みの Editor JSON 再反映に切り替えた。

1. 関連の追加を含む場合、確認ダイアログの前に `ExportModelUnit` で図の Editor JSON を退避する。**プロジェクトが保存済みであること**が条件（未保存なら C220 で停止。自動保存はしない）。
2. トランザクション内で `Relate` → 製品が不可視のコネクタを作る。
3. 退避した Editor JSON に、そのコネクタと同じ `Id`・`ModelId`・両端で、既存の線を雛形（`DefinitionId`・Style・Labels をコピー）にした要素を `IsVisible: true` で足し、`ImportUnitFromJson` で Editors だけ再反映する。
4. 再反映後にコネクタ数が変わらない（同じ ID で上書きされた）ことを照合し、変わっていれば停止して取り消す。

削除側は変更なし。

## 0.3.4: 表示の操作を ShowShape に切り替え

0.3.3 の `SetVisible(true)` は SDK の読戻しでは真になったが、確定・保存後の Editor JSON では `IsVisible: false` のままで線は描かれなかった。手動で引いた線との差は `IsVisible` だけ（`DefinitionId`・Style・Labels は同じ）。新規コネクタには `IDiagram.ShowShape` を先に呼び、それでも偽なら `SetVisible(true)` を試す。どちらで真になったかを診断に残す。

## 0.3.3: 追加した関連の線を表示にする

0.3.2 の診断で、`Relate` の直後に製品が作るコネクタは Association モデル付き・両端の座標も正しいが `IsVisible=False` だと分かった（モデルのプロパティには関連先が入っている）。この実行で新しく現れたコネクタのうち不可視のものを `SetVisible(true)` で表示にし、読戻しで真になることを確認する（偽のままなら `ShowShape` を試し、それでも偽なら停止して取消）。削除側は変更なし。

## 0.3.2: 増えたコネクタの中身を診断に出す

実機で `Relate` 1 回の追加が試行・確定とも通り、SDK 上はコネクタが 59→60 に増えたが、図では線が見えなかった。原因を切り分けるため、この実行で新しく現れたコネクタについて両端のモデル名・コネクタのモデル種別・LineType・IsVisible・両端ノードの座標・折れ点数を診断ファイルに出す（`new connector ...` 行）。比較や書込みの規則は変えていない。

## 0.3.1: 対の行を自動で補う

実機で `UnRelate("Related")` 1 回で関連が丸ごと消え、逆側の `RelateFrom` の行も無くなり、コネクタも 59→58 に減ることが分かった（線の図形は関連に追従する）。0.3.0 は片側の行だけ消した入力を「入力にあり図にない」として止めていたが、対のフィールド名は `GetRelationsOf` で分かるので、入力に残っている対の行は「一緒に消える」扱いで照合する。追加も同様に、`Relate` の後で対のフィールドを観測し、入力に無ければ補って照合する。同じ関連への 2 回目の操作は「対の側で処理済み」として読み飛ばす。

これで、片側の 1 行を足す・消すだけで関連を追加削除できる。両方書いてあっても通る。

## 0.3.0: 既存クラス間の関連の追加削除

ボタン名を「更新を試行して戻す」「更新を確定」に変えた（ID は同じ）。対象に、図に載っている既存クラス間の参照関連の追加と削除を加えた。

| 操作 | 書き方 | 前提 |
|---|---|---|
| 追加 `A --> "0..*" B : Field` | `A.Relate("Field", B)` | `Field` が A のメタクラスの参照フィールドで、B がその型に入ること。同じ関連が無いこと。多重度上限に空きがあること |
| 削除（行を消す） | `A.UnRelate("Field", B)` | 読取りがフィールド由来の線であること（コネクタ由来の `--` は対象外） |

**両端フィールドの関連に注意。** 実機では `Related` と `RelateFrom`、`Children` と `Parent`、`Parts` と `Whole`、`SuperClasses` と `SubClasses` が 1 本の関連の両端で、PlantUmlTool はそれぞれの側から 1 行ずつ、合計 2 行を出す。片側だけ追加・削除すると製品が逆側も変えるため、読戻しで「入力にあり図にない」「図にあり入力にない」の残差が出て試行は取り消される。停止メッセージに残差の行が PlantUML の形で出るので、その行を入力に足す・消すと通る。0.3.0 は片側から書いたときに逆側がどうなるか（逆側フィールド・コネクタ図形の自動生成）を観測する版でもあり、診断ファイルに `after add: relations ...` と `connectors on the diagram: N -> M` を記録する。

多重度やロール名の変更、片端が新しいクラスの関連は引き続き停止する。

### 実機手順（0.3.0）

1. 原本のコピーで、既にある行 `SIProtocolInfrastructure --> "0..*" SITransportInfrastructure : Related` と対になる `SITransportInfrastructure <-- "0..*" SIProtocolInfrastructure : RelateFrom` の **2 行を両方削除**して「更新を試行して戻す」。期待は関連削除 2 件で 3 行一致。診断ファイルの `connectors on the diagram` を控える。
2. 逆に、原本のコピーに関連のない 2 クラス間へ `X --> "0..*" Y : Related` と `Y <-- "0..*" X : RelateFrom` の 2 行を足して「更新を試行して戻す」。期待は関連追加 2 件で 3 行一致。
3. 片側だけ（`Related` の 1 行だけ）足した入力で試行し、停止メッセージの残差の行を確認する。

## 0.2.2: 可視性と型の更新

「改名を試行して戻す」「改名を確定」の対象を、属性・操作の**名前・可視性**と属性の**型**に広げた（ボタン名は据え置き）。既定値・多重度・操作の引数や戻り値の変更、追加・削除・移動は引き続き C231 で全体を停止する。

| 項目 | 書き方 | 前提 |
|---|---|---|
| 名前 | `SetField("Name")` | 0.2.0 で実機確認済み |
| 可視性 | `Visibility` へ記号に対応する値（`+`→`Public`、`-`→`Private`、`#`→`Protected`、`~`→`Package`）を `SetField` | Public / Private は実機で観測済み。Protected / Package は UML の名前で未確認。記号を消す変更は扱わない |
| 型 | `Type` 参照を `UnRelate` → `Relate` で既存の型モデルへ付け替え | 入力の型名と同じ名前の型モデル（`Type` 系メタクラス）がプロジェクト内に 1 件あること。同名が複数なら同じクラス配下→上位の所有先の順で絞り、決まらなければ停止。無ければ停止（型モデルの新規作成は扱わない）。型を空にする変更は扱わない |

いずれも書込み前に現在値が読取りと一致することを確認し、書込み後に読戻しと図全体の再読取りで照合する。

### 実機手順（0.2.2）

1. `クラス図_class_test.puml` で属性の可視性を変える。例: 17 行目の `- m_ConvertErrCode` を `+ m_ConvertErrCode` に。「改名を試行して戻す」で UPDATE-C000 の 3 行が一致することを確認する。
2. 型を変える。例: 18 行目の `- m_AppName : std::string` の型を、同じクラスの別の属性が使っている既存の型名に変える（`std::vector<ConvertErrCodeType>` など）。存在しない型名（`std::vector<int>`）は C220 で停止するはず。
3. 試行が通ったら「改名を確定」→ Ctrl+Z / Ctrl+Y → 保存して開き直し →「差分を検証」0 件。

## 0.2.1: 同じクラスで複数の改名

実機で属性 1 件と操作 1 件を同時に改名した入力が、追加・削除に落ちて C231 で停止した。0.1.2 で属性と操作を同じ種類として比較するようにした結果、「同じ親で対応の付かない要素が両側 1 件ずつなら改名」の規則が 2 件で一意にならなくなっていた。

改名の対応付けを 3 段にした。名前以外の描画（可視性・型・引数など）が同じで両側一意、次に属性・操作それぞれの種類で 1 対 1、最後にメンバ全体で 1 対 1。属性と操作の同時改名、型の違う属性 2 件の同時改名をローカル検査に追加した。

0.2.0 の改名の試行・確定・本体の Undo/Redo・保存再読込後の差分 0 件は実機確認済み。

## 0.2.0: 属性・操作の改名を書き戻す

0.1.4 で未編集出力との差分 0 件、改名 1 件の入力で `update attribute [name]` 1 件を実機確認したので、最初の書込みを入れた。対象は**属性・操作の名前だけ**。型・可視性・関連・クラスの追加削除は扱わず、混在していれば全体を停止する（C231）。

| ボタン | 動作 |
|---|---|
| 改名を試行して戻す | 差分が改名だけなら、トランザクション内で `SetField("Name")` → 読戻し → 図全体を SDK で読み直して入力と 0 件 → **必ず取消** → 処理前と一致することを照合 |
| 改名を確定 | 同じ照合が通ったときだけ Commit し、エディタを再描画する。Undo/Redo・保存再読込は手動確認 |

実行前に、対象モデルが編集可能で、現在の名前が読取りと一致することを確認する。確認ダイアログの後にも図が変化していないことを再検査する。自動保存はしない。失敗時は取消を 1 回試み、復元を照合する。復元が確認できなければ保存せずにコピーを開き直す。

### 実機手順

1. `クラス図_class.puml` をコピーし、属性名を 1 つ書き換える（例: `m_ConvertErrCode` → `m_ConvertErrCode2`）。
2. 「改名を試行して戻す」でそのファイルを選ぶ。結果画面の UPDATE-C000 で「一時適用と照合: 一致」「取消API: 正常終了」「復元照合: 一致」を確認する。
3. 成功したら「改名を確定」で同じファイルを選ぶ。UPDATE-C001 で「確定: 成功」「確定後の再照合: 一致」を確認し、図の表示が変わっていることを見る。
4. リボンの Undo で戻り、Redo で再反映されることを確認する。コピーを保存して開き直し、「差分を検証」で同じファイルを指定して 0 件を確認する。

## 0.1.4: 残った link 12/12 の原因

`_current.puml` と PlantUmlTool 出力の diff で確定した。配置先の PlantUmlTool は DeSIDE のフィールド名に矢印を割り当てている（`Children` → `o--`、`SubClasses` → `<|--`、`RelateFrom` → `<--`、`Whole` → `--*` など）。0.1.1 で既定表から外したのは誤りだったので戻した。矢印は比較しない方針のまま。

対応付けの第1段が両端・ラベル・矢印の一致を要求していたため、同方向で矢印だけ違う2本（`Children` と `SubClasses`）が両方外れ、第2段では2対2で一意にならず残っていた。第1段はラベルだけで対応付ける。

## 0.1.3: 残った link 12/12 の切り分け用

0.1.2 の実機比較で class・attribute・operation は 0 件になり、link の追加 12 / 削除 12 だけが残った。入力側は同じ2ノード間の連続2行が6組。診断ファイル（`_preview_...txt`）の add / delete 行に、線の両端の別名・ラベル・多重度・フィールド名を出すようにした。比較規則は変えていない。

## 0.1.2: 未編集出力との比較で出た 86 件への対応

実機の未編集出力との比較で class 移動 40・attribute 削除 9・operation 追加 11 / 削除 2・link 追加 12 / 削除 12 が出た。いずれも出力側の規則を読取りに写し切れていなかったもの。

- class の入れ子: PlantUmlTool は class 系ノードの中の子ノードを、親の中括弧に入れず同じ深さに書く（クラスの中にクラスは書けない）。読取りも同じ形に展開し、container（package / component キーワード）の中だけを入れ子として扱う。
- メンバの種別: 型定義（PointerType 等）は属性として出力されるが、名前に括弧を含む（`decltype(...)`）のでテキストからは操作に見える。メンバは描画した1行の文字列で比較し、属性・操作・リテラルの種別だけの違いは差分にしない。
- 匿名フィールドの線: ラベルなしの同じ行が同じ2ノード間に複数並ぶ。区別できない行は件数の少ない方まで順に対応付け、余りだけを追加・削除にする。片側が匿名の統合線（`A -- B :  / Parent`）も2本に分解する。

## 0.1.1: 実機の調査結果を反映

- 括弧を含む属性（`+ raw : uint8 (unsigned)` や `data(old)` のような名前）を操作と誤読して E120 で止まっていた。操作は「名前(引数)」の直後が行末か ` : 戻り値` の行だけにした。
- 矢印は比較しない。Next Design では関連の種類がフィールド名で決まり、矢印は出力側の対応表が付けた見た目なので、`-->` と `--|>` の違いは差分にしない。両端・ロール名・多重度は比較する。
- DeSIDE のフィールド名（SuperClasses など）を矢印表から外した。PlantUmlTool にもその表がなく、実機出力は `-->` で出るため、`_current.puml` を出力と一致させるには既定表を同じにする必要がある。
- E120 で止まったときは、該当行の前後を診断ファイルに記録する。
- 実機の観測（ERDiagram / ノード3種のメタクラス / 結合フィールド一覧）はローカル知識に保存した。

Next Design V3.1.9向けのC#スクリプト拡張。PlantUMLを正本にして既存クラス図を差分更新することを目標にした実験で、[シーケンス生成実験](../SequenceImportProbe/README.md)と同じ進め方を別拡張で繰り返す。**0.1.0は読取り専用。** 図・モデル・プロジェクトを一切変更しない。

## 0.1.0: クラス図調査と差分検証

クラス図は `EditorType` が `ERDiagram`（プロジェクトによっては `TreeDiagram`）で、シーケンス図の `ISequenceDiagram` のような型付きAPIがない。`IDiagram` から取れるのはノードとコネクタだけなので、クラス・属性・操作・関連の意味はモデル側のメタクラス名とフィールド名から読む。その対応表（`ClassSyncOptions`）はプロファイル依存で、実機の値はまだ記録していない。だから最初の版は「実機の値を観測する」と「読取りが出力と一致することを確かめる」の2つに絞る。

| グループ | ボタン | 動作 |
|---|---|---|
| 結果・診断 | 診断表示 / 結果表示 | 直前の実行結果を表示する |
| 調査 | クラス図調査 | 表示中のクラス図のメタモデルと図形・コネクタ・関連を出力ウィンドウ（カテゴリ `ClassImportProbe`）と診断ファイルへ書き出す。保存済みなら図の Editor JSON も退避する |
| 比較 | 差分を検証 | PlantUMLファイルを選び、表示中の図と比較して差分候補を件数表で表示する。図は変更しない |

### クラス図調査

出すもの: `EditorType`・`ViewDefinitionName`・図モデルのメタクラスと所有連鎖、ノードの座標とモデル、ノード・子・孫のメタクラス名ごとの全フィールド（種別・型・多重度・現在値）、コネクタごとの両端・`LineType`・`GetRelationsOf` の関連情報、図上のノードを結ぶ参照・所有フィールドの一覧、既定の対応表で読んだ結果と要照合項目。

プロジェクトが保存済みなら `ExportModelUnit` で図のユニットを一時ファイルへ出力し、その図の `Editors` 要素だけを `_editor.json` に残す。書込み設計に使う。未保存なら省略して続行する。この操作は自動保存しない。

### 差分を検証

入力は **PlantUmlTool の「表示中の図を出力」で出したクラス図の `.puml` を編集したもの**。解析できる文法はその出力形式に限る。

```
@startuml
title 図名
hide empty members

package "所有元" {
  class "名前" as 別名 <<ステレオタイプ>> {
    + 属性 : 型 [多重度] = 既定値
    --
    + 操作(引数 : 型) : 戻り値
  }
}

別名 --> "0..*" 別名 : ロール名
@enduml
```

- 宣言は `class` / `abstract class` / `interface` / `enum` / `entity` / `struct` / `package` / `component`。`"名前" as 別名` の形が基本で、引用符や別名のない `class Foo` も受け付ける。
- 矢印は `-->` `--|>` `..|>` `..>` `o--` `*--` `--` `..` と、左向きの `<|--` `--*` `<--` を受ける。矢印の向きは解釈せず、行の左右をそのまま関連元・関連先とする。矢印の種類は比較しない。
- 双方向の統合線（`a "1" -- "0..*" b : x / y`、または左側にも多重度がある行）は、出力側の統合規則の逆で2本の関連に分解する。前のラベルが左→右、後ろが右→左。
- `hide` / `skinparam` / `!theme` などの表示指定と `'` コメントは無視する。無視した行は診断ファイルに記録する。
- 解釈できない行、未宣言の別名、閉じ括弧の不足は `E120: N行目:` で止まる。

比較は名前で対応付ける。クラスは所有先（package か図上の親ノード）と名前・キーワード、メンバはクラス内の種類と名前、関連は両端とロール名。同じ親の中で1件だけ対応が付かない要素は改名として扱う。結果画面は種類×操作の件数表、診断表示は入力行と種類だけで、設計本文・名前・IDは実行PCの診断ファイルにしか出さない。

クラスの並びは図形の座標で決まるため、宣言順の違いは差分にしない。メンバの並び替えと所有先の変更は `move` として出す。

## 配置と操作

1. `ClassImportProbe` フォルダを他の拡張と同じ `extensions` フォルダへコピーし、Next Design を再起動する。
2. **コピーしたプロジェクト**でクラス図をメインエディタに表示する。
3. 「クラス図調査」を押す。結果画面と「診断表示」のスクショ、および出力ウィンドウ（カテゴリ `ClassImportProbe`）の全文を渡す。診断ファイルは `%LOCALAPPDATA%\NextDesign.ClassSync\reports\` に `..._probe_....txt`（同じ名前の `_editor.json`）として残る。
4. PlantUmlTool の「表示中の図を出力」で同じ図を `.puml` に出力する。
5. 「差分を検証」でその未編集の `.puml` を選ぶ。差分候補 0 件が目標。残差が出たら、診断ファイルと同名の `_current.puml`（拡張が図から書き出したPlantUML）を PlantUmlTool の出力と比較すると、読取り側のどこが違うか分かる。

診断ファイルにはモデル名・ID・設計内容が含まれる。社内の管理ルールに従って扱い、公開リポジトリへ追加しない。

## 既知の制約

- 図への反映はしない。属性・操作の本文更新、関連の追加削除、クラスの追加削除は 0.2.0 以降で、実機のメタモデル記録を取ってから段階的に検証する。
- `ClassSyncOptions` の対応表は PlantUmlTool の既定表と同じにしてある（出力と一致させるため）。未対応のメタクラスは属性、未対応のフィールドは `-->` として読み、要照合項目に出す。
- PlantUmlTool の出力と同じく、図に載っているノードだけを対象にする。自己参照は線にしない。所有関連（`IsEmbedded`）は線にしない。
- 同じ名前のメンバが1クラスに複数ある場合、対応付けは兄弟列の並びに依存する。
- 統合線の分解規則は出力側の規則の逆算なので、手書きで `A "1" -- "0..*" B : x` と書くと双方向として読む。
- PlantUmlTool の版によって関連の矢印表現が変わる（DeSIDE の矢印表の有無で `o--` / `<|--` になるか `-->` と統合線になるか）。比較では矢印を見ないのでどちらの出力も同じに扱えるが、`_current.puml` とのテキスト比較は矢印表付きの形に合わせてある。

## 開発

`sync/` が正本で、`main.cs` の生成区画へ `bundle.py` が埋め込む。生成区画は直接編集しない。

```powershell
python ClassImportProbe/sync/bundle.py
python ClassImportProbe/tests/run_tests.py --sdk-root work/sequence-api-research
python C:\Users\ksk01\.claude\skills\nextdesign-extension\scripts\validate_manifest.py ClassImportProbe --nd-version 3
```

テストは `samples/*.puml` の解析・書出しの往復（バイト一致）、差分計画の件数、画面文言、版の一致を検査する。`--sdk-root` は公式SDKに対するコンパイルで、APIの実在を確認するだけ。実機での読取り結果は保証しない。

残作業・実機ゲート・監査台帳は[実装計画](../docs/class-sync-plan.md)を参照する。
