OpenProjectOptions.ReadOnly プロパティ
=================================

名前空間: NextDesign.Desktop

説明​
-----------------------

読み取り専用でプロジェクトを開くかを指定できます。

True が指定された場合、開いたプロジェクトは編集できません。  
既定値は False です。

    bool ReadOnly { get; set; }

型​
--------------------

*   bool