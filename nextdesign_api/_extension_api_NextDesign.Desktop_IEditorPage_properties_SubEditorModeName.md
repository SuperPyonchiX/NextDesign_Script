IEditorPage.SubEditorModeName プロパティ
===================================

名前空間: NextDesign.Desktop

説明​
-----------------------

アクティブタブのサブエディタの表示モード名

SubEditorModeが「Custom」の場合、`Custom.\{ModelEditorCategory.Id\}` の形式となる。

    string SubEditorModeName { get; }

型​
--------------------

*   string