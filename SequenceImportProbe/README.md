# シーケンス生成実験 0.7.0

Next Design V3.1.9向けのC#スクリプト拡張。PlantUMLからの新規図生成と、対応表を使った既存図のメッセージ本文更新・削除を扱う。0.6.8までの本文更新は実機確認済み。0.7.0の既存図への削除適用は実機未確認。

## 0.7.0: メッセージ削除

「メッセージを差分更新」で、本文更新と次の削除を一つのトランザクションで反映する。

- 図側にだけ追加され、正本のPlantUMLにないメッセージ。
- 対応表作成後にPlantUMLから削除したメッセージ。

前回入力と今回入力を列として照合し、残すメッセージのモデルIDを対応表から引き継ぐ。削除後の空白は詰めず、残す図形の座標を維持する。識別子コメントはPlantUMLへ追加しない。

削除するモデルに接続する関連も削除する。関連先のクラスなどのモデルは残す。実行区間、参加者、分岐、Note等は削除しない。それらの追加削除・変更や、メッセージ追加・移動・送受信先変更は今回の範囲外。複数図に表示されるメッセージの同時削除も未対応。

### 実行手順

1. `SequenceImportProbe` フォルダを配置先で更新し、Next Designを再起動する。結果画面の版が0.7.0であることを確認する。
2. 検証用プロジェクトのコピーで対象図を開く。既存の `.ndmap.xml` は再作成せず使える。
3. 「メッセージを差分更新」で、完成状態としたいPlantUMLと対応表を選ぶ。
4. 更新件数・削除件数を確認して実行する。結果に削除件数と保持照合の結果を表示する。プロジェクトは自動保存しない。
5. 同じ入力でもう一度実行し、更新API未呼出になることを確認する。
6. 保存後のGit差分で残す要素のID・座標を確認し、Undo/Redoと再読込も確認する。Undoした図には更新前の `.bak` を使うか、図に合う対応表を作り直す。

最初は、すでに検出できた図側の余剰メッセージ1件で確認する。その後、本文だけの更新が従来どおり動くこと、連続5件のメッセージ行を削除しても後続が書き換わらないことを確認する。実行区間の `activate` / `deactivate` や分岐の行は今回の検証では残す。

### 図情報の保持と検証範囲

削除前に公式SDKの `ExportModelUnit` で現在のユニットを一時ファイルへ出力し、対象EditorのJSON全体を取得する。削除対象のMessages項目だけを除き、未知の属性やStyleも保持してEditorだけを反映する。元のプロジェクトファイルを直接書き換えたり、保存済みファイルを現在状態とみなして読み込んだりしない。出力ファイルは処理後に削除する。

エクスポートで対象Editorが取得できない場合や、現在の図と図形ID・配置が一致しない場合は、モデルを削除しない。JSON以外の出力は扱わない。出力形式、プロジェクト直下／分割ユニット、未保存の編集内容を含む出力の実機挙動は未確認。

反映後は削除モデル・図形の消失、残すID・関連・座標、取得したEditor属性、関連先モデルの生存を照合する。失敗時は取消し、取得できた情報で復元を照合する。対応表は成功後に更新し、前回分を `.bak` に残す。

ローカルでは1件・5件・10件・全メッセージ削除、削除と改名の混在、分岐間の同文メッセージ、再実行、Editorの未知属性保持をテストする。公式SDKコンパイルとmanifest検査も実施する。実機の削除・保存Git差分・Undo/Redo・再読込は別途確認が必要。

## 過去の版の記録

以下は各版の時点での範囲と検証状況。現在の範囲は上記0.7.0の説明を参照する。

既存の `ModelUpdateProbe` とは別フォルダで配置する。明示的なCommit/Rollbackの後にDisposeで終了処理を重ねない実装を継続する。Undo/Redoと保存後の再読込は実機で確認する。

0.2.1では、見本の図に複合フラグメントがある場合、その具体的な型と分岐の型を使用する。複数の型が混在するときは自動選択せず停止する。見本にない場合は従来のフィールド型を使う。シェイプ数の不一致は種類ごとの期待数・取得数を表示する。

0.2.2では、条件表示と後続要素の間に余白を設け、入れ子の枠を親の枠より内側に配置する。実機の文字サイズによる表示は別途確認する。

0.2.3ではrefも見本の図の具体型を使用する。refを取り込む際は、同期メッセージとref（相互作用の利用）がある図を開く。見本がない場合はインポート前に停止する。見本の参照先・本文はコピーしない。具体型による表示改善は実機で検証する。

0.2.4ではNoteも見本の図の具体型を使用し、その型から本文フィールドを取得する。Noteを取り込む際は、同期メッセージとNoteがある図を開く。見本がない場合はインポート前に停止する。見本の本文はコピーしない。複数のNote型がある場合は自動選択せず停止する。実機の生成・本文表示は別途検証する。

0.2.5では縦方向の固定余白を縮小する。メッセージ間隔は75から50、条件下の余白は50から40へ変更し、分岐の末尾・枠の前後・ライフライン末尾も詰める。Noteとrefの高さは本文行数から計算し、複数行メッセージと条件には行数分の余白を加える。既存の図には適用されず、取り込み直して生成する図に反映される。実機の文字重なりは別途確認する。

0.3.0では実行区間、自己メッセージ、返信を追加する。`skinparam sequenceMessageAlign`、`maxMessageSize`、`sequenceReferenceBackgroundColor` の単行指定は受け付けるが、見た目には反映せずNext Designの既定表示を使う。この差は取り込み前に表示する。新規構文の実機表示は未確認。

0.3.2では図外からの受信を追加する。`samples/08-incoming.puml` で外部信号から始まる実行区間と返信を検証できる。参加者1本の図にも対応する。送信元フレームと接続IDを照合するが、実機表示は未確認。

0.3.1では `A ->] : 本文` の右側への図外メッセージを追加する。受信先を新しい参加者ではなく図のフレームへ接続する。非同期・返信の矢印も扱う。`samples/07-outgoing.puml` で単独検証できる。受信先のフレームと接続IDを照合するが、実機表示は未確認。

0.3.6では `destroy A` によるライフラインの破棄点を追加する。既存のメッセージ種別を維持し、独立した破棄点として生成する。実行区間は破棄位置で閉じ、破棄後の送受信・二重破棄は事前検査で停止する。破棄後のactivate/deactivateは0.3.9から警告付きで除外する。`loop` の外で始めた実行区間を内側で閉じる配置も扱う。検証用は `samples/09-destroy.puml`。生成データ・公式SDKコンパイルは検査済み、破棄点の実機生成・表示は未確認。

0.3.7では破棄点からライフラインへの関連方向を修正する。破棄点側から関連定義を取得し、生成JSONの送信元・送信先の型をテストする。修正後の実機取り込み・表示は未確認。

0.3.8では破棄後再利用のエラーに対象の別名・破棄行・使用構文を表示する。「診断表示」には選択ファイルのパスと事前検査エラー行の前後を記録する。スレッドを破棄した後に別の参加者が返信する回帰テストを追加した。この並びの誤判定はローカルで再現しておらず、今回の変更は原因特定のための診断強化。

0.3.9では旧エクスポートに残る破棄後の `activate/deactivate` を除外する。除外件数と行番号を取り込み前の確認画面と結果に表示し、破棄点を維持する。破棄後の実際の送受信・二重破棄は引き続き停止する。実機取り込みは未確認。

0.3.10では破棄点の表示用の型を現在の図のビュー定義から取得する。見本がないときに汎用の基底型を使う処理をやめ、定義が複数ある場合は既存破棄点の型で選択する。特定できなければ取り込み前に停止する。破棄点の数の不一致には期待数と取得数を表示する。実機での改善は未確認。

## 現在の図の更新（0.5.2・実験）

「現在の図を更新（実験）」はPlantUMLを正として図全体を置き換える。図モデル・フレーム・フレーム所有関連・エディタ・フレーム図形のIDを維持し、ライフライン以下は新しいIDで再作成する。手作業の配置やPlantUMLにないNoteは引き継がない。

1. プロジェクトのコピーを開き、更新したいシーケンス図を表示する。現時点では、型情報を取得するため異なるライフライン間の同期メッセージが必要。
2. 「現在の図を更新（実験）」から更新後のPlantUMLと診断の保存先を選ぶ。
3. 対象の図名、生成内容、置換対象の件数を確認して実行する。
4. UPDATE002でID・要素・図形の照合結果を確認し、図を開き直して表示を確認する。

子要素と外部モデルとの関連も解除して置き換える。クラスや操作などの外部モデル自体は削除対象に含めず、更新後に存在を照合する。新しい子要素への関連の張り直しは行わない。図本体の関連は更新後にも照合する。削除と再取り込みは同じトランザクションに入れ、不一致や例外では取消を試み、旧モデルの名前・関連・図形IDの復元を診断に記録する。自動保存はしない。`before.txt` は診断用の構造記録で、単独で復元できるバックアップではない。

実機で確認済みなのは0.4.0の一時図に対する同一IDの名称更新と、一時モデルを含む取消まで。0.5.2の子要素削除・追加、既存エディタへの反映、外部参照保持、保存再読込、Undo/Redoは実機未確認。

「更新APIを検証」は従来どおり、一時図で同じIDへの名称更新を試し、最後に取り消すUPDATE001を実行する。既存図の更新には「現在の図を更新（実験）」を使う。

## 配置と操作

1. `git pull` 後、`SequenceImportProbe` フォルダを他の拡張と同じ `extensions` フォルダへコピーする。ZIPの展開やビルドは不要。
2. Next Designを再起動する。
3. **コピーしたプロジェクト**を開く。異なるライフライン間の同期メッセージがある既存シーケンス図をメインエディタに表示する。
4. 「シーケンス生成実験」タブの「PlantUMLを取り込む」を押す。
5. まず同梱の `samples/01-async.puml` を選ぶ。コピーであることの確認後、ログの保存先を選ぶ。
6. 取り込み件数を確認して実行する。JSONやIDの入力は不要。
7. 元の図と同じ親の配下に追加された `Async sample` を開く。結果画面と新しい図のスクショを渡す。
8. 順に `02-fragments.puml`、`03-ref.puml`、`04-note.puml` を試す。個別に動いたら `05-all.puml` で混在を確認する。サンプルの書き換えは不要。
9. 実行区間・自己呼び出し・返信は `06-activation-self-reply.puml` で検証できる。実際のUTF-8 PlantUMLファイルも同じ操作で選べる。

「最小図を生成」も残している。PlantUMLからの生成で問題が出たときに、最小構成との比較に使える。

停止したら結果画面を渡す。原因の説明が足りない場合は「診断表示」で詳細を開ける。詳細にはプロファイルの属性名やIDが含まれる場合がある。ファイルを編集する必要はない。

失敗後は保存せずに検証用コピーを開き直してから再実行する。取消APIが正常終了しても、プロジェクト全体の復元が確認できたとは扱わない。

## 取り込み対象

| 要素 | この版の入力・動作 |
|---|---|
| 参加者 | `participant A`、`participant "表示名" as A`。メッセージ中の暗黙宣言も扱う |
| 同期 | `A -> B : request()` |
| 非同期 | `A ->> B : notify()`。同期と別のMessageSortで保存・照合 |
| 図外からの受信 | `[-> A : 本文`、`[->> A : 本文`、`[--> A : 本文`、`[-->> A : 本文`。受信先の約60px左にある独立メッセージ端から接続。実線の `->>` は非同期、破線は返信として扱う |
| 返信 | `A --> B : result`、`A -->> B : result` をReplyとして取り込む |
| 図外宛て | `A ->] : 本文`、`A ->>]`、`A -->]`、`A -->>]`。0.3.3では送信元から約60px左の独立メッセージ端へ接続する。フレーム・別ライフラインへは接続しない。消失記号の構文は未対応 |
| 自己メッセージ | `A -> A : operation()` など。送受信の高さをずらした折り返しを生成 |
| 破棄 | `destroy A`。独立した破棄点を生成し実行区間を閉じる。再生成と破棄メッセージ専用矢印は未対応 |
| 実行区間 | `activate A` / `deactivate A`。受信直後のactivateは受信先の実行区間を使用。入れ子を閉じると外側へ戻る |
| 表示設定 | `skinparam sequenceMessageAlign left/center/right`、正整数の `maxMessageSize`、6桁16進色の `sequenceReferenceBackgroundColor`。Next Design既定表示で代替する旨を確認画面に表示 |
| 複合フラグメント | `alt / else / end`、`opt`、`loop`、`par`、`break`、`critical`、`group`。条件・分岐と入れ子を生成。対応する演算子がプロファイルにない場合は実行前に停止 |
| 相互作用の利用 | `ref over A, B : 本文`、複数行の `end ref`。この版は枠と本文のみで、別の図への参照先は設定しない |
| Note | `note over A, B`、`note left of A`、`note right of B`。一行または `end note` で囲む本文。位置を生成し、アンカー線は生成しない |

UTF-8、1ファイル1図、300KB以下、参加者1〜50本、要素500件以下、入れ子8段まで。`@startuml` と `@enduml` が必要。左向き矢印、include、上表以外の表示設定・装飾指定などは未対応で、行番号を表示して停止する。未対応行を飛ばして図を作ることはしない。`activate/deactivate` は図内または各分岐内で対応させる。`loop` の境界をまたぐ開始・終了は扱う。`destroy` は対象の実行区間をまとめて閉じる。alt等の分岐をまたぐ実行区間の開始・終了や、色付きactivateはこの版では扱わない。

現在の図の型・構造関連・列挙値を取得し、新しいIDのモデル・関連・シェイプを生成する。`BeginUndoTransaction(false)` 内で `IProject.ImportUnitFromJson` を呼び、APIの警告・エラーがなく、読戻し照合が通った場合に確定する。Note本文がRichTextの場合は、同じトランザクション内で `SetRichTextField` を使って設定する。

照合するのは図の所属・名前、ライフラインとメッセージのシェイプ数、送受信先・種別・本文・実行区間への接続・送受信の高さ、実行区間の長さ、複合フラグメントの演算子と分岐の条件・所属・メッセージ、refとNoteのシェイプ数・本文。枠の位置や入れ子の表示、非同期矢印の見た目はスクショで確認する。API照合に成功しても、描画・Undo/Redo・保存後の再読込が確認済みになったとは扱わない。

この版は新規図への取り込み。既存図のIDや参照関係を維持した差分更新は未実装。

## 仮説と根拠

入力構造は[公式サンプルのプロジェクト](https://github.com/denso-create/NextDesign-Samples/blob/main/ndcli-extensions/ModelValidation/project/先進運転システムソフト開発.nproj)の保存形式を参照した。同梱の検証用PlantUMLは架空の内容とする。複合フラグメントと分岐の保存形式は公開サンプルに基づく。ref・Noteの保存形式と入れ子の関連付けには未検証の仮説が含まれる。プロファイル情報で事前検査し、生成後の読戻しと実機表示で切り分ける。

JSON形式で保存されたプロジェクトでは、先頭の `SchemaVersion` を利用する。取得できない保存形式では公式サンプルに見られた `13.0` を仮の値として使い、その判断をログに残す。保存形式の互換性、既定のシェイプスタイル、インポート時の整合性検査は実機で切り分ける。[API調査記録](../docs/sequence-import-api-research.md)も参照。

## 記録

保存先に `sequence_日時_ID` フォルダを作り、次を保存する。機密情報を含む可能性があるため会社PC内で扱う。

| ファイル | 内容 |
|---|---|
| `source.puml` | 選択したPlantUMLの内容。取り込み時のみ |
| `input.json` | 実際に渡す新規モデル・関連・シェイプ |
| `before.txt` | スキーマの選び方・SDK・親と見本のID |
| `checked.txt` | 確定直前の照合結果。照合完了時のみ |
| `result.txt` | 最終結果・API診断・例外・取消の結果 |

## 開発側の検査

```text
python SequenceImportProbe/tests/run_tests.py
python SequenceImportProbe/tests/run_tests.py --sdk-root work/sequence-api-research
python <skills>/nextdesign-script-extension/scripts/validate_manifest.py SequenceImportProbe --nd-version 3
```

1つ目はPlantUMLの解析、同期・非同期・返信種別、自己折り返し、実行区間の入れ子と復帰、分岐・入れ子、refとNoteの生成、未対応構文の拒否に加え、生成JSONのID独立性、所有構造、送受信、シェイプ、座標、文字列エスケープと不正入力を検査する。2つ目はさらに公式 `NextDesign.Core / Desktop 3.1.3.30714` と `.NET 6` 参照アセンブリで配布スクリプト全体をコンパイルする。SDKは開発PCの作業フォルダにのみ配置する。

これらの検査ではNext Designを起動しない。インポートの成功、失敗時の復元、実機の表示は保証しない。

図外矢印の構文は[PlantUML公式のIncoming and outgoing messages](https://plantuml.com/sequence-diagram)に基づく。Next DesignのSDKでは受信ポートにFrameを指定できる。0.3.3では希望する終了表示に合わせ、受信先にMessageEndを指定する。PlantUMLの右端表現をそのまま再現する配置ではない。

## 0.3.3: 終了メッセージを短い左向きに配置

既存入力の `A ->] : 本文` を変更せず、送信元の実行区間から約60px左に独立したメッセージ端を配置する。別ライフラインや図のフレームには接続しない。同期・非同期・返信の種類と本文は維持する。図外からの受信は従来どおり左側フレームから接続する。

端点の型はプロファイルの所有フィールドと、見本図にメッセージ端がある場合はその具体型から取得する。取り込み後にモデル・シェイプ数、受信先がMessageEndであること、ライフラインへ接続していないこと、端点の座標を照合する。不一致なら既存の取消処理で停止する。

ローカルではJSONの位置・接続検査と公式V3.1.3 SDKに対するコンパイルを確認。実機での丸い端点、矢印、文字位置は未確認。更新・再起動後に同じ入力を取り込み、最後のメッセージが短く左へ伸びることを確認する。既に生成した図は書き換えない。

根拠: [V3 IMessage の受信ポートと消失メッセージ](https://docs.nextdesign.app/extension/v3.x/api/NextDesign.Core/IMessage/)。

## 0.3.4: 外部からの受信も近傍の独立端点から接続

`[-> A`、`[->> A`、`[--> A`、`[-->> A` は、受信先の実行区間の約60px左に独立した送信端を置き、短い右向きのメッセージとして生成する。送信端をライフラインや図のフレームに接続しない。同期・非同期・返信の種類と、受信直後のactivateによる実行区間の再利用は維持する。

取り込み後は送信ポートの型・接続先・端点座標を照合する。ローカルの生成データ検査と公式SDKでのコンパイルを実施し、実機表示は更新・再起動後の再生成で確認する。

## 0.3.5: 独立端点の高さをメッセージに揃える

図外への送信・図外からの受信で、独立端点に適用していた上方向5pxの補正を削除した。端点のY座標をメッセージのSourceY・TargetYと同じ値にし、取り込み後にも3つの座標を照合する。左方向の距離は約60pxのまま。生成データ検査と公式SDKコンパイルで確認し、実機の水平表示は再生成して確認する。

### 0.5.1 の変更

全体置換の対象は、相互作用が所有する子モデルから判定する。SDK の `IInteractionElement` として取得できないことだけを理由に停止していた E141 の判定を削除した。子モデルにはライフライン、メッセージ、実行区間、Noteなどの内部データが含まれる。図・フレームのID保持と、図の外との関連の検査は継続する。実機での全体更新は未確認。

### 0.5.2 の変更

子要素の外部関連で停止するE142を削除した。所有されている子要素だけを削除し、外部の関連先モデルが残っていることを更新後に照合する。図本体の関連とIDは維持する。外部関連を持つ図の更新・取消は実機未確認。

## 0.5.3: 差分更新のAPI検証

差分更新へ移行する前に、コピーしたプロジェクトで「差分更新APIを検証」を実行する。PlantUMLの選択は不要。同期メッセージがある図を開いて型情報を取得する。

UPDATE003は一時図を作り、同じIDへの名称更新、メッセージ1件だけを含むJSONの追加、追加したメッセージだけの削除を試す。削除中だけ公式SDKの `SuspendModelVerification()` を使い、スコープ終了時に検査を復帰させる。既存のモデル名・関連ID・図形IDを照合し、最後に一時図を含む操作を取り消す。自動保存しない。

これは既存図の差分更新機能ではなく、部分JSONの取り込みと削除手段を確認する実験。実機未確認であり、Git差分の最小化や図形座標の保持を実証するものではない。UPDATE003の結果と診断表示を確認してから、既存図への適用へ進む。全体置換へ自動的に切り替える処理は設けない。

差分更新の設計は [DIFFERENTIAL_UPDATE.md](DIFFERENTIAL_UPDATE.md) を参照。

## 0.5.4: 差分追加時の図形入力を補完

UPDATE003の追加JSONに、一時図を生成したときのフレーム・既存図形を同じID・値で含める。追加モデル1件と関連3件だけをEntities/Relationsに渡す。Editorsの部分指定が変換処理の例外原因という仮説に対する修正で、実機は未確認。既存図形の全データ保持と追加図形1件をローカルで検査した。追加APIが例外で結果を返さない場合は、直前の名称更新のsuccessを表示しない。操作は引き続き「差分更新APIを検証」。

## 0.5.5: 削除後のモデルと図形を分けて確認

UPDATE003では削除後に図を再取得し、対象モデルの有無・削除済みフラグ、相互作用内のメッセージ数、図形の不足・余剰を記録する。追加したモデルが消え、追加図形だけが残った場合に限って、元の図形データを同ID・値で再反映する。既存図形に別の差異がある場合は停止する。モデル・関連はこの再反映に含めない。実機での削除完了と取消は未確認。操作は引き続き「差分更新APIを検証」。

## 0.6.0: コメントを付けない本文差分更新

PlantUMLを変更せず、対応表（.ndmap.xml）を別ファイルで管理する。既存図のメッセージ本文だけを、ID・関連・配置を維持して更新する最初の版。追加・削除・移動、参加者・条件・Note等の変更にはまだ対応しない。全体置換は「全体置換（旧実験）」へ改称し、自動では呼ばない。

1. プロジェクトのコピーを保存し、更新対象の図を開く。
2. 「差分対応表を作成」で、現在の図に対応する**更新前**のPlantUMLを選ぶ。
3. 同じ種別・送受信先・本文のメッセージは出力順に自動対応する。対応表を社内の作業フォルダへ保存する。この段階では図もPlantUMLも変更しない。
4. PlantUMLのコピーでメッセージ本文を1件だけ変更する。識別子コメントは不要。
5. 「本文を差分更新」で更新後のPlantUMLと対応表を選ぶ。
6. 変更前後を確認して実行する。図の表示、保存前後のGit差分を確認する。自動保存はしない。

現在の図の本文とPlantUMLが同じならモデル更新APIを呼ばない。0.6.2では、前回からPlantUMLが変わっていなくても図側の本文が違えばPlantUMLへ合わせる。既存の対応表はそのまま使える。ID・関連・配置を変更せず本文だけ更新するが、図全体の意味の一致を保証する処理ではない。

成功時は対応表を更新し、前回分を.bakに残す。更新確定後に対応表の置換だけ失敗した場合は.pendingを次回選べる。対応表は社内情報を含むため公開リポジトリへ追加しない。

ローカルでは差分判定・対応表の整合性・原子的な保存とバックアップ・公式SDKコンパイルを検証する。既存図への本文差分更新と保存Git差分は実機未確認。

## 0.6.2の確認項目

- 図側だけで本文を変更し、同じPlantUMLを再入力する。本文がPlantUMLへ戻ることを確認する。
- 図とPlantUMLで別々の本文を変更する。対応付け済みの全メッセージ本文がPlantUMLに一致することを確認する。
- 図側で配置を調整する。本文更新でその座標が変わらないことを確認する。
- 更新後に同じ入力を再実行する。本文更新が0件になることを確認する。

0.6.1の「図側だけの本文変更を残す」挙動を修正した。最終要件はPlantUMLを正として意味上の差分を反映し、同じ要素のID・配置を可能な限り保持すること。現在の実装は本文だけで、追加・削除・順序・送受信先・条件等の構造同期は未実装。配置の変更でもメッセージの順序や所属が変われば意味上の差分となるため、構造同期では保持対象と区別する必要がある。

## 0.6.3: 初回の参加者対応付け

参加者名は完全一致を優先し、一致がなければ改行・連続空白・コロン前後の空白を整えて照合する。大文字小文字、単語の区切り、コロンの個数は区別する。表示名や元のPlantUMLは書き換えない。

候補がない場合や複数ある場合は、表示名・モデル名・X位置・接続メッセージ例を提示して対応先を選ぶ。「いいえ」で次の候補へ進む。対応済みの参加者は候補から外す。全候補を断るとモデルを変更せずキャンセルする。選択した対応は後続のメッセージ照合にも使う。

完全一致・正規化後の候補件数と選択IDを診断に残す。今回の修正は初回対応付けの改善であり、構造差分の同期を追加するものではない。実機での対応表作成は未確認。

## 0.6.4: 出力したPlantUMLとのメッセージ対応付け

PlantUmlToolの出力はメッセージ図形のTextと図形側の送受信ライフラインを使う。0.6.3までの対応付けはモデルNameとモデル側の送受信先を使っていたため、表示がモデル名と異なるメッセージを照合できなかった。0.6.4では出力側と同じ表示文字列・空白正規化・矢印への種別変換・図形側の送受信先を使う。

本文更新の差分判定も表示文字列で行う。出力直後の同じ本文をモデルNameへ書き戻さない。本文を実際に変更した場合は、従来のName書込み後に表示文字列も照合する。型情報などから表示を合成する図でName変更だけでは目的の表示にならない場合は取消し、成功とは扱わない。表示を構成する複数属性の更新は未実装。

「差分対応表を作成」で出力したままのPlantUMLを選び、作成後に同じファイルで「本文を差分更新」を実行する。期待結果は本文差分なし。実機での照合成功は未確認。不一致時の診断には、入力行の種別・本文と、図側のモデル名・表示本文・送受信先を記録する。

## 0.6.5: 重複メッセージを自動確定

初回対応付けで、同じ種別・送受信先・本文の候補が複数あっても確認画面を出さない。未使用の一致候補をPlantUML出力と同じ順序（送信Y、ポートX、図形IDの順）で選び、入力の出現順に対応付ける。一度対応したモデルを別の行へ再利用しない。候補数と選択IDは診断に記録する。

出力したままのPlantUMLを基準入力とする。候補が存在しない場合の診断と、参加者名が特定できない場合の候補選択は残す。今回の変更はメッセージの重複候補確認だけが対象。既存の対応表は作り直す必要がない。

## 0.6.6: 対応表用の解析を生成用検査から分離

対応表作成・本文比較・対応表の保存と再読込には、既存図を照合するための解析を使う。分岐をまたぐactivate/deactivateなど、新規図の実行区間生成では扱えない配置も、その行を削除せず読み取る。破棄後の実行区間行も原形のまま保持する。

構文・参加者・フラグメント終端・入力上限の検査は継続する。実行区間の配置を再生成したり、妥当性を保証したりする変更ではない。本文更新では実行区間行の追加削除も構造差分として検出し、無視しない。新規図生成の検査は従来どおり。

出力したままのPlantUMLで「差分対応表を作成」を再実行し、その対応表と同じ入力で本文差分なしになるか確認する。分岐をまたぐ入力の対応表XML往復・本文比較・構造変更検出・SDKコンパイルは検証済み。実機確認は未実施。

## 0.6.7: 図側に追加要素がある場合の対応表作成

図とPlantUMLの参加者・メッセージ数が違うだけでは停止しない。PlantUMLにある要素を現在の図へ対応付け、対応表に含まれない図側の参加者・メッセージ数を結果へ表示する。モデルと図形の同じIDは二重に数えない。入力側の要素の対応先が存在しない場合は、引き続き該当要素を診断する。

対応表作成では図を変更しない。図側だけの追加要素をPlantUMLに合わせて削除する処理は未実装で、対応表作成成功は構造同期完了を意味しない。本文更新でも対応表に含まれない図側メッセージ数を表示する。

図側の追加も正本へ含めたい場合は、追加後にPlantUMLを再出力して対応表を作る。追加前のPlantUMLを正本とする場合は、そのファイルで既存要素の対応表を作成できるが、追加分の削除は構造同期の実装が必要。

## 0.6.8: 初回対応付けでも改名を差分として検出

本文の完全一致を対応付けの必須条件にしない。送受信先と種別が同じメッセージ列を、出力順に照合する。一致する本文を優先しながら、改名と図側への途中追加を列全体で対応付ける。同点では先の出現を選ぶ。名前が違うという理由だけでE163にはしない。

結果に「本文の差分を検出」の件数を表示し、診断に入力本文・図の本文・選択IDを記録する。対応表作成では図を変更しない。その対応表で本文更新を実行すると、現在の表示本文とPlantUMLを比較して更新する。

識別子のない初回照合なので、同じ経路の全メッセージを同時に改名・追加削除・並べ替えた入力から元の意図を保証することはできない。対応表が既にある場合は、再作成せずそれを使えばモデルIDで追跡できる。送受信先・種別・並びに対応する要素そのものがない場合の再作成は未実装。
